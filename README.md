# An app enabling the user to:

  - Find out the base rate for a given currency
  - Calculate a final currency amount between two currencies after entering an initial value and selecting the currencies required
